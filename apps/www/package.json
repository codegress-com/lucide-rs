{
  "name": "lucide-rust-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "trunk serve",
    "build": "trunk build --release",
    "css": "tailwindcss --input ./public/style.css --output ./public/output.css --watch",
    "css:build": "tailwindcss --input ./public/style.css --output ./public/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0-alpha.30",
    "@tailwindcss/cli": "^4.0.0-alpha.30"
  }
}
